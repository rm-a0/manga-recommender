"""Pydantic request and response models, one module per resource.

Each resource defines a `<Resource>Summary` for list items and embedding, and a
`<Resource>Detail` for the single-resource response. The README's "Project
structure" section records which shape may embed which.
"""
